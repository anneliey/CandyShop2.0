@page
@model CandyShop.Pages.ChipsProductsModel
@{
}
@section SearchBar {
    <partial name="_searchBar" for="SearchTerm" />
}
<div class="container">
    <br />

    <div class="row">

        @foreach (var product in Model.ChipsProductView.Concat(Model.ChipsDbProductView))

        {
            <div class="col-3">
                <a asp-route-id="@product.Id" asp-page="/ProductDetails">
                    <img class="product-thumbnail" src="~/images/@product.ImageUrl" />
                </a>
                <div class="product-body">
                    <p class="all-products-title"> @product.Name</p>
                    <p>I lager: @product.Stock</p>

                </div>
                <div class="add-to-cart-buttom">
                    <a class="btn btn-primary mb-4" asp-route-id="@product.Id" asp-page="/Cart">Add to cart</a>
                </div>
            </div>

        }
    </div>
</div>
