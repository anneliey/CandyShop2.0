@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}


<div class="container">
    <div class="d-flex justify-content-center">
        <img class="header" src="~/images/BonBon.png"/>
    </div>
</div>

    <div class="container">
        
    
        <h1 class="text-center"><strong>『Nya produkter』</strong></h1>
       
            <div class="row justify-content-center">
                @foreach (var product in Model.IndexProductView)
                {
                    <div class="col-3">
                        <a asp-route-id="@product.Id" asp-page="/ProductDetails">
                            <img class="product-thumbnail" src="~/images/@product.ImageUrl" />
                        </a>
                        <div class="product-body">
                            <p class="all-products-title"> @product.Name</p>
                        </div>
                    </div>
                }
            </div>
        </div>
    
    <div id="imageCarousel" class="carousel slide" data-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active">
                    <img class="d-block w-100" src="images/Slide1.jpg" alt="First slide">
                </div>
                <div class="carousel-item">
                    <img class="d-block w-100" src="images/Slide2.jpg" alt="Second slide">
                </div>
            </div>
        </div>
       
